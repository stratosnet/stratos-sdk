export const generateMasterKeySeed = mnemonicArray => {};

export const encryptMasterKeySeed = (password, masterKeySeed): string => '';

export const decryptionMasterKeySeed = (password, encryptedMasterKeySeed) => {};

export const getMasterKeySeedAddress = (masterKeySeedPublicKey): string => '';

export const getMasterKeySeedPublicKey = masterKeySeed => {};

export const unlockMasterKeySeed = (password, encryptedMasterKeySeed) => {};
